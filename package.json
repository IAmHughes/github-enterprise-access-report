{
  "name": "user-access-report",
  "version": "1.0.0",
  "description": "Generate a csv of a given user's access in an Enterprise",
  "main": "user-access-report.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "IAmHughes, jwiebalk",
  "license": "MIT",
  "dependencies": {
    "@octokit/graphql": "*",
    "dotenv": "*",
    "yargs": "*",
    "fs": "*"
  }
}
